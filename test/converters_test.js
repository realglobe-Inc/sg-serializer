/**
 * Test case for converters.
 * Runs with mocha.
 */
'use strict'

const converters = require('../lib/converters.js')
const assert = require('assert')
const co = require('co')

describe('converters', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Converters', () => co(function * () {

  }))
})

/* global describe, before, after, it */
